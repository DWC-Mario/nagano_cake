 <div class="container">
   <table>
    <tr>
      <th><h1>商品一覧</h1></th> <td><h4>(全<%= @items_count.count%>件)</h4></td>
    </tr>
   </table>
    <div class="row">
      <!--<div class="col-lg-2">-->
      <!-- %= render "public/genres/search", genres: @genres %>-->
      <!--</div>-->



  <% @items.each do |item| %>
  <!--<div class="col-md-3">-->
  <div class="item-box">

      <%= image_tag item.get_image(200,200) %><br>
     <%=link_to item.item_name, item_path(item.id) %><br>
     ¥<%= item.price %>



  </div>
  <!--</div>-->
  <% end %>
</div>
  <%= paginate @items %>

  </div>
</div>
